{
  "name": "portfolio-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "node scripts/sync-content.js",
    "predev": "npm run sync",
    "dev": "astro dev",
    "prebuild": "npm run sync",
    "build": "astro build",
    "postbuild": "node scripts/apply-cv-flag.js",
    "preview": "astro preview",
    "deploy": "npm run build && node scripts/deploy-gh-pages.js",
    "check:images": "node scripts/check-images.js",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.3.5"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "axe-core": "^4.13.0",
    "puppeteer-core": "^25.12.0"
  }
}
